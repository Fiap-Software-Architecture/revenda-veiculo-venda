server:
  port: 8082

spring:
  application:
    name: venda-service
  datasource:
    url: jdbc:h2:mem:vendadb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
